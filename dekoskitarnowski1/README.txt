To compile gcc mypstree.c -o mypstree
To run  ./mypstree -[PID number]
The program intakes a PID number and outputs all of the children processes for the given PID.
All required features have been implemented correctly and there are no missing features.